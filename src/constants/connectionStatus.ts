import type { ConnectionStatus } from "../types";

export const STATUS_CONNECTING: ConnectionStatus = "connecting";
export const STATUS_CONNECTED: ConnectionStatus = "connected";
export const STATUS_DISCONNECTED: ConnectionStatus = "disconnected";
export const STATUS_ERROR: ConnectionStatus = "error";
